<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="site.forum.web.teacher.mapper.CourseMapper">

    <select id="selectCourseByStatusAndId" resultType="site.forum.web.common.data.po.CourseInfo">
    SELECT c.* FROM course c WHERE c.status = #{status} AND c.tid = #{tid}
    </select>
</mapper>